@model IEnumerable<TeleTime.Models.Shift>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ShiftName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ShiftName)
        </td>
        <td>
            @*@Html.ActionLink("EditName", "Edit", new { id = item.ID }) |*@ <!-- TODO - Known bug with this one. -->
            @Html.ActionLink("DetailsAboutShift", "DetailsAboutShift", new { id = item.ID }) |
            @Html.ActionLink("EditWorkShift", "ShowShift", "WorkShift", new { id = item.ID }, null) |
            @Html.ActionLink("Delete", "Delete", new { id = item.ID })
        </td>
    </tr>
}

</table>
